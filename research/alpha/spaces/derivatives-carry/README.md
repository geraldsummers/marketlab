# Derivatives state and carry

Funding prediction, directional return prediction from derivatives state, basis
convergence, and delta-neutral carry are separate candidates. The existing
funding-only next-hour return diagnostic was inconclusive and does not test the
registered carry theory.

This space should accumulate the synchronized state needed to distinguish an
expensive crowded position from a genuinely executable carry opportunity.
